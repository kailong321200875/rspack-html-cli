import './index.less'

document.getElementById('app').innerHTML = '这是首页的 JavaScript 内容'

function addNumber(a, b) {
  return a + b
}

addNumber(1, 2)

const object = {
  name: 'zhangsan',
}

object.name = 'lisi'
